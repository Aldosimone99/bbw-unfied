# Legacy migrations archive

These migrations came from the first `bbw-transition`-derived database
baseline. They are preserved for historical reference and possible future
domain porting, but Supabase does not apply this directory.

The active database source of truth is the numbered sequence under
`apps/backend/supabase/migrations/`. New schema changes must use a new migration
there and must not be added to this archive.
