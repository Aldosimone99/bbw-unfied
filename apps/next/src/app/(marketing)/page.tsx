import LandingClient from "../../components/layout/LandingClient";
import { landingHtml } from "../../components/layout/landingMarkup";

export default function Home() {
  return <LandingClient html={landingHtml} />;
}
